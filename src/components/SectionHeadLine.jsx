export default function SectionHeadLine({ heading, description }) {
    return (
        <div class="row">
            <div class="title">
                <h6>{heading}</h6>
                <p>{description}</p>
            </div>
        </div>
    )
}
